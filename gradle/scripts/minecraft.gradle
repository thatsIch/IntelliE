// needed for pulling new versions from forge
minecraft {
    version = "$mcversion-10.13.2.1230"
    replace "@version@", projectversion.toString()
    runDir = rundir
}

task cleanRun(type: Delete) {
    delete rundir
}